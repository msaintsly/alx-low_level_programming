#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (successful)
 */

int main(void)
{

	char joy;

	for (joy = 'a';joy <= 'z'; joy++)
		putchar (joy);
	for joy = 'A'; joy <= 'Z'; joy++)
		putchar (joy);
	putchar ('\n');
	return (0);
}
