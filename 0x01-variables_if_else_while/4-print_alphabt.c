#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - point of entry
 *
 * Return: Always 0 (Successful)
 */

int main(void)
{
	char lower_case, e, q;

	e = 'e';
	q = 'q';

	for (lower_case = 'a'; lower_case <= 'z'; lower_case++)
		if (low != e && low != q)
			putchar (lower_case)

	putchar('\n');

	return (0);
}
